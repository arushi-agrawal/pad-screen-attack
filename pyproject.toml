[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pad-screen-attack"
version = "0.1.0"
description = "Screen-attack presentation attack detection prototype: folder of face images in, CSV of attack scores out"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["pad"]
